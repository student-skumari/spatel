<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Birthday Scanner Bar</title>
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: 'Comic Sans MS', cursive;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background: linear-gradient(-45deg, #ff9a9e, #fad0c4, #a1c4fd, #c2e9fb);
      background-size: 400% 400%;
      animation: gradientBG 10s ease infinite;
    }

    @keyframes gradientBG {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .scanner-box {
      text-align: center;
      border: 2px solid #0ff;
      padding: 30px;
      border-radius: 20px;
      box-shadow: 0 0 30px #0ff;
      width: 300px;
      background-color: rgba(0,0,0,0.6);
      animation: pulseBorder 2s infinite;
    }

    @keyframes pulseBorder {
      0% { box-shadow: 0 0 20px #0ff; }
      50% { box-shadow: 0 0 40px #0ff; }
      100% { box-shadow: 0 0 20px #0ff; }
    }

    .scanner-box h2 {
      margin-bottom: 20px;
      color: #0ff;
    }

    .scan-area {
      width: 150px;
      height: 150px;
      margin: 0 auto;
      background: repeating-linear-gradient(
        to bottom,
        #0ff 0px,
        #0ff 2px,
        transparent 2px,
        transparent 4px
      );
      animation: scanLines 1.5s infinite linear;
      border-radius: 50%;
      border: 2px solid #0ff;
      margin-bottom: 20px;
    }

    @keyframes scanLines {
      from { background-position: 0 0; }
      to { background-position: 0 100px; }
    }

    #scanBtn {
      padding: 12px 25px;
      font-size: 1rem;
      border: none;
      border-radius: 10px;
      background-color: #0ff;
      color: #000;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.3s;
    }

    #scanBtn:hover {
      background-color: #00f0f0;
    }

    #progress {
      display: none;
      width: 100%;
      background-color: #222;
      margin-top: 15px;
      border-radius: 10px;
      overflow: hidden;
    }

    #bar {
      height: 10px;
      width: 0%;
      background: #0ff;
      transition: width 2s ease;
    }

    .slider {
      width: 350px;
      height: 450px;
      perspective: 1000px;
      position: relative;
      display: none;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      z-index: 1;
    }

    .slider h1 {
      margin-bottom: 10px;
      font-size: 2rem;
      color: #fff;
      text-shadow: 2px 2px 4px #000;
    }

    .slider .wish {
      font-size: 1.2rem;
      color: #fff;
      margin-bottom: 20px;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.05); }
      100% { transform: scale(1); }
    }

    .slides {
      width: 100%;
      height: 100%;
      position: absolute;
      transform-style: preserve-3d;
      animation: rotateSlider 15s infinite linear;
    }

    .slide {
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: 20px;
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
      overflow: hidden;
      transform-origin: center center;
    }

    .slide img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .slide:nth-child(1) { transform: rotateY(0deg) translateZ(400px); }
    .slide:nth-child(2) { transform: rotateY(72deg) translateZ(400px); }
    .slide:nth-child(3) { transform: rotateY(144deg) translateZ(400px); }
    .slide:nth-child(4) { transform: rotateY(216deg) translateZ(400px); }
    .slide:nth-child(5) { transform: rotateY(288deg) translateZ(400px); }

    @keyframes rotateSlider {
      0% { transform: rotateY(0deg); }
      100% { transform: rotateY(-360deg); }
    }

    audio { display: none; }

    .balloon-container {
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      width: 100%;
      overflow: hidden;
      z-index: 0;
      pointer-events: none;
    }

    .balloon {
      position: absolute;
      width: 60px;
      height: 80px;
      border-radius: 50%;
      opacity: 0.8;
      animation: float 10s linear infinite;
    }

    @keyframes float {
      0% { transform: translateY(100vh) scale(0.8); opacity: 0; }
      50% { opacity: 1; }
      100% { transform: translateY(-10vh) scale(1); opacity: 0; }
    }

    #confettiCanvas {
      position: fixed;
      top: 0;
      left: 0;
      pointer-events: none;
      z-index: 0;
    }

    #starsContainer {
      position: fixed;
      top: 0;
      left: 0;
      height: 100%;
      width: 100%;
      pointer-events: none;
      z-index: 0;
      overflow: hidden;
    }

    .star {
      position: absolute;
      width: 8px;
      height: 8px;
      background: white;
      border-radius: 50%;
      box-shadow: 0 0 5px #fff;
      animation: twinkle 2s infinite ease-in-out alternate;
    }

    @keyframes twinkle {
      0% { transform: scale(1); opacity: 1; }
      100% { transform: scale(0.3); opacity: 0.3; }
    }
   
  </style>
</head>
<body>

<!-- Scanner Box -->
<div class="scanner-box" id="scanner">
  <h2>🎛 Birthday Identity Scanner</h2>
  <div class="scan-area"></div>
  <button id="scanBtn" onclick="startScan()">Start Scan</button>
  <div id="progress">
    <div id="bar"></div>
  </div>
</div>

<!-- Balloons, Confetti, Stars -->
<div class="balloon-container" id="balloonContainer"></div>
<canvas id="confettiCanvas"></canvas>
<div id="starsContainer"></div>

<!-- Birthday Wishes Slider -->
<div class="slider" id="sliderBox">
    <h1><b>23 birthday wishes </b></h1>
  <h1> <i>Happy Birthday to you, childhood friend! </i>🎂</h1>
  <div class="wish">Wishing you joy, love, and cake! Have a magical birthday! god bless you🎈</div>
  <div class="slides">
    <div class="slide"><img src="birthday boy.jpeg"></div>
    <div class="slide"><img src="birthday boy 2.jpg"></div>
    <div class="slide"><img src="birthday boy 3.jpg"></div>
    <div class="slide"><img src="birthday boy 4.jpg"></div>
    <div class="slide"><img src="birthday boy 5.jpg"></div>
  </div>
</div>

<!-- Audio -->
<audio id="scanSound">
  <source src="https://assets.mixkit.co/sfx/preview/mixkit-robotic-scanner-973.mp3" type="audio/mpeg">
</audio>

<audio id="birthdayMusic" loop>
  <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
</audio>

<script>
  function startScan() {
    const scanSound = document.getElementById("scanSound");
    const birthdayMusic = document.getElementById("birthdayMusic");

    scanSound.play().catch(e => console.warn("Scan sound error:", e));
    birthdayMusic.play().catch(e => console.warn("Music error:", e));

    // Animate progress bar
    document.getElementById("progress").style.display = "block";
    const bar = document.getElementById("bar");
    bar.style.width = "0%";
    setTimeout(() => { bar.style.width = "100%"; }, 50);

    setTimeout(() => {
      document.getElementById("scanner").style.display = "none";
      document.getElementById("sliderBox").style.display = "flex";

      const container = document.getElementById("balloonContainer");
      const colors = ['#ff4d6d', '#ffbe0b', '#3a86ff', '#8338ec', '#06d6a0', '#ef476f'];

      for (let i = 0; i < 25; i++) {
        const balloon = document.createElement('div');
        balloon.className = 'balloon';
        balloon.style.left = Math.random() * 100 + '%';
        balloon.style.backgroundColor = colors[i % colors.length];
        balloon.style.animationDelay = Math.random() * 5 + 's';
        balloon.style.animationDuration = 6 + Math.random() * 6 + 's';
        container.appendChild(balloon);
      }
    }, 2000);
  }

  // Confetti Animation
  const canvas = document.getElementById('confettiCanvas');
  const ctx = canvas.getContext('2d');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;

  let confetti = Array.from({ length: 100 }, () => ({
    x: Math.random() * canvas.width,
    y: Math.random() * canvas.height - canvas.height,
    size: Math.random() * 8 + 4,
    speed: Math.random() * 3 + 2,
    color: `hsl(${Math.random() * 360}, 100%, 60%)`
  }));

  function drawConfetti() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    confetti.forEach(p => {
      ctx.fillStyle = p.color;
      ctx.fillRect(p.x, p.y, p.size / 2, p.size);
      p.y += p.speed;
      if (p.y > canvas.height) p.y = -p.size;
    });
    requestAnimationFrame(drawConfetti);
  }
  drawConfetti();

  // Star Animation
  const starContainer = document.getElementById('starsContainer');
  for (let i = 0; i < 30; i++) {
    const star = document.createElement('div');
    star.className = 'star';
    star.style.left = `${Math.random() * 100}%`;
    star.style.top = `${Math.random() * 100}%`;
    star.style.animationDelay = `${Math.random() * 5}s`;
    starContainer.appendChild(star);
  }
</script>
</body>
</html>
